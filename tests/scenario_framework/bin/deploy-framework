#!/usr/bin/env bash

# Scenario Framework Deployment Wrapper
# This script runs the framework deployment CLI using Deno.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
FRAMEWORK_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

deno run --allow-all "$FRAMEWORK_ROOT/scripts/deploy_cli.ts" "$@"
