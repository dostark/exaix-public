## Step 30.1: Extend RequestOptions Interface

**Action:** Add `flow` field to `RequestOptions` interface in `src/cli/request_commands.ts`.

**Success Criteria:**
- [ ] `RequestOptions` interface includes `flow?: string` field
- [ ] TypeScript compilation passes without errors
- [ ] Interface documentation updated

**Test Description:** Unit test verifies interface accepts flow parameter without breaking existing functionality.

---

## Step 30.2: Update Request Creation Logic

**Action:** Modify `RequestCommands.create()` method to:
1. Accept flow parameter in options
2. Validate flow exists in `Blueprints/Flows/` directory
3. Add flow field to YAML frontmatter when specified
4. Ensure mutual exclusion: cannot specify both `flow` and `agent`